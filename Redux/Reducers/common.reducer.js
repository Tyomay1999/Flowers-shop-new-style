const initialState = {
    loading: false,
    message: '',
}

export const commonReducer = (state = initialState, action) => {
    switch ( action.type ){
        default:
            return state
    }
}